import { TabStub } from "@/components/layout/TabStub";

export default function Page() {
  return (
    <TabStub
      title="Projects"
      description="Card grid from Core/Context/Projects.md sections with progress bars derived from Todos whose tags match each project."
    />
  );
}
