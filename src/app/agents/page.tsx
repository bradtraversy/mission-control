import { TabStub } from "@/components/layout/TabStub";

export default function Page() {
  return (
    <TabStub
      title="Agents"
      description="Mission statement banner plus Travis, Claude, and any sub-agents from Core/Workflows/*/. Each card has a Ping button that queues a task."
    />
  );
}
