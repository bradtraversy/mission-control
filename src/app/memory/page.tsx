import { TabStub } from "@/components/layout/TabStub";

export default function Page() {
  return (
    <TabStub
      title="Memory"
      description="Reverse-chronological view of memory/*.md with day headers and expandable entries."
    />
  );
}
