import { TabStub } from "@/components/layout/TabStub";

export default function Page() {
  return (
    <TabStub
      title="Network"
      description="Machines, disks, services, and automation panels rebuilt from Network/data/*.json. Replaces the Python network-dashboard once at parity."
    />
  );
}
