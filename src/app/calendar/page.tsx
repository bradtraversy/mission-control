import { TabStub } from "@/components/layout/TabStub";

export default function Page() {
  return (
    <TabStub
      title="Calendar"
      description="Google Calendar agenda on top (from Calendar/next-7-days.json cache), Agent Schedule weekly grid on bottom with Always Running chip row."
    />
  );
}
